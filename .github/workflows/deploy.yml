name: Build & Deploy

on:
  push:
    branches: [main]

permissions:
  contents: write
  pages:    write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.2.2
          bundler-cache: true

      - name: Install Gems
        run: |
          bundle install --jobs 4 --retry 3
          bundle exec jekyll build --destination _site

      - name: Debug Tailwind output
        run: |
          echo "-- ls on your output dir --"
          ls -R _site/assets/css || true
          echo "-- head of main.css (if it exists) --"
          head -n20 _site/assets/css/main.css || true

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: "npm"      # ← only here!

      - name: Build CSS
        run: npm ci && npm run build:css

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir:   ./_site
          publish_branch: gh-pages
