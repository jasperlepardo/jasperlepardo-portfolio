{
  "name": "jekyll-tailwind",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tailwind": "npx tailwindcss -i ./assets/css/main.css -o ./_site/assets/css/styles.css --minify --watch",
    "build:css": "tailwindcss -i ./assets/css/main.css -o ./assets/css/styles.css --minify",
    "build": "npm run build:css && bundle exec jekyll build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.10",
    "tailwindcss": "^3.4.1"
  }
}
